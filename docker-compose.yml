version: '3'
services:
  db:
    image: kartoza/postgis:9.4-2.1
  mongo:
    image: mongo:3.4
  web:
    build: .
    # command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/app
    ports:
      - "5432:5432"
      - "27017:27017"
    depends_on:
      - db
      - mongo